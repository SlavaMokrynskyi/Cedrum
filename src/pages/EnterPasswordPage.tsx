import React from "react";
import EnterPassword from "core/components/EnterPassword/EnterPassword";

export default function EnterPasswordPage() {
    return (
        <div>
            <EnterPassword />
        </div>
    );
}